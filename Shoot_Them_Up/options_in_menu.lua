-- Ecran d'options qui sera accessible à partir du menu.

local Settings = require("options_assets")

local Options_M = {}

function Options_M.Init()
  
end

function Options_M.Update(dt)
  
end

function Options_M.Draw()
  love.graphics.print("Options du menu.")
end

function Options_M.Keyboard(key)
  
end

function Options_M.Mouse(x, y, button)
  
end

return Options_M